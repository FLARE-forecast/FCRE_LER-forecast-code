This is the directory which contains the git submodules.

After cloning the directory, from the terminal you will need to execute the following commands:
```
git submodule init
git submodule update
```
